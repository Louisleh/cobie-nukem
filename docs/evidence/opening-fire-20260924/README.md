# WCB-008K — five-second ordinary-input shot probes (candidate only)

Source: candidate branch `codex/saturday-level1-opening-20260919`, commit `b827ce3`; both runs bind the same runtime/harness content digest `087579b407e684de6685a0c29059c9158650d3735a0bb2e0e29cb195cf5b2dff`. Godot 4.7.1. Commands: `python3 tools/evidence/salmon_opening_probe.py --out <fresh scratch destination> --seconds 5` and the same with `--tablet`. The wrapper validated each full 50-frame output, engine log, source identity, actual isolated user/save roots, clocks and PNG dimensions before these small selected-frame copies were retained. The full 50-frame scratch outputs were deliberately removed after validation; archived `runtime.json` still lists their hashes and is **not** a standalone re-verifiable 50-PNG archive. Selected frame hashes can be checked against their entries in `runtime.json`.

- Desktop native 1280×720: 150 measured process frames, 300 physics ticks, 50 image samples; sampled player Z 10.000 → -1.935. `fire_primary` left-button press/release scheduled at frames 90/93; Pawstol `fired` signal at process frame 103, ammo 15 → 14, cost 1. 1024×768 native 4:3 (`tablet/`) reproduced the same successful event/ammo sequence and frame/tick counts.
- The event-submission receipt plus observed weapon signal proves an ordinary input-driven **shot**, not target contact; the player input callback is not independently instrumented. The shot may miss or hit world geometry. The later frame shows an active Groundskeeper and Gull and 95 health, but does not prove a shot hit either actor. No enemy-contact or 30-second continuous-run claim.
- Human pixel inspection of selected desktop and 4:3 native frames: no blank/black frame or obvious 4:3 HUD clipping. The field and shed are plainly too flat and sparse for release-ready art; dark HUD/portrait occupy a large lower region. This is diagnostic inspection, not taste/feel or physical-device acceptance.
- `frame_0000.png`, `frame_0090.png`, `frame_0147.png` are sampled keyframes at each aspect. `receipt.json`, `runtime.json`, `source.json`, `engine.log` preserve each probe's structured report. These are native debug probes, not packaged Web, human, or 30-second combat-playthrough evidence. Release performance gates remain red; exports deleted to recover disk must be rebuilt and revalidated before any package claim.
