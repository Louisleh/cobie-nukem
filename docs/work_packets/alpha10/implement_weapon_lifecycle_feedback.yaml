work_id: alpha10-weapon-lifecycle-feedback
role: spark-gameplay-worker
goal: Implement the existing combat contract's explicit lowering/raising transitions using WeaponFeelProfile timings, preserve one deterministic queued selection, and route rich CombatFeedbackEvent metadata into the HUD/crosshair without removing legacy signals.
baseline_revision: 6e1669bf04333f25b2a14ae76a2091ad1b2857fc
dependencies: [alpha10-audit-combat-heroes]
owned_paths: [scripts/combat/weapon_base.gd, scripts/player/player_controller.gd, scripts/ui/hud.gd, scripts/ui/crosshair.gd, tests/unit/combat_test_runner.gd]
read_only_context: [docs/design/combat.md, scripts/combat/weapon_feel_profile.gd, resources/weapons]
forbidden_paths: [scripts/core/build_info.gd, docs/PHASE_ROADMAP_PRD.md, project.godot, export_presets.cfg, .github]
behavioral_contract: Weapon requests queue once; selection index/HUD update is deterministic; no blank/flicker frame; firing is blocked until READY; reload cancellation preserves ammo; feedback distinguishes enemy, kill, destructible, world, miss while legacy kind remains compatible.
acceptance_criteria: [RAISING and LOWERING have real timed behavior, duplicate requests are idempotent, switch durations come from feel resources, crosshair differentiates kill/destructible, existing ammo/reload/firing behavior stays green, focused test covers lifecycle and rich feedback]
focused_tests: /opt/homebrew/bin/godot --headless --path . --script res://tests/unit/combat_test_runner.gd
evidence_required: Cohesive commit, exact test output, acceptance-by-item summary.
out_of_scope: Balance changes, new assets, release metadata, deployment.
time_limit_minutes: 35

Use only owned paths. Run a headless editor import before the focused test. Commit one cohesive change and return the Spark result packet.
